#!/bin/bash

echo "To uplod script from local repo to remote repo"

echo " git add <filename>; git status (to check which files are currently in staging area)"

echo " git commit -m "message" (moving the file from staging area to local repo)"

echo " git push origin main  (moving file from local repo to remote repo)"